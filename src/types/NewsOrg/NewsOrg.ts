export type SourceType = {
    id: string,
    name: string,
    category?: string,
    country?: string,
    description?: string,
    language?: string,
    url?: string
}

export type SourceModel = {
    label: string,
    value: string
}